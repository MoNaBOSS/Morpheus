# Morpheus 1.0.1 — Fluid arrival

## Scope

- Visible personalized welcome after boot for existing profiles.
- Full workspace or explicit tray choice; no automatic microphone enablement.
- Companion header shortcut reopens welcome without resetting configuration.
- Larger original filament Signal, stronger Matrix boot field, shorter smooth
  setup and page transitions, reduced-motion support.
- Activation final screen waits for a decision instead of disappearing.
- Speech stop/supersession settles playback and suppresses stale provider audio.
- Main rejects tray handoff if no associated live tray exists.

No planner, execution permission, OpenClaw or provider credential authority is
changed. This is a Windows experience update, not a claim that all long-term
Morpheus capabilities are implemented. Local signing remains CI-dependent.

## Validation

- Runtime source: `7336fba7a705d3e7536896aed0924f2fe3c7d018`.
- Typecheck, lint (0 errors, 12 inherited warnings), harness, comms replay/compare
  and 702 Morpheus unit tests across 75 files pass.
- 18 focused Electron journeys pass. Affected visual flows were repeated after
  the final artwork changes (11/11), and voice-settings navigation after its final
  change (2/2). The latter asserts visible controls and focus, not a brittle exact
  pixel offset while asynchronous settings panels load.
- Full repository unit run: 2,658 passed, 2 skipped, 18 failures. Sixteen are
  previously recorded Windows path/plugin/upgrade fixtures; two are chat timing
  failures and the chat file passes 5/5 in isolation. Their source files were
  unchanged by this release. Do not describe the repository-wide suite as green.
- Normal-production unpacked smoke: exact executable/version verified; live
  filament transforms change; returning greeting renders; real Main tray handoff
  hides the window; test restores it through BrowserWindow; voice settings remain
  unchanged; a real system report completes; Gateway reaches ready and Chat opens.
- No page errors or fatal Main messages observed. Quit leaves no Morpheus process
  or listener on 18789. No installer was run during verification.
- Existing profile opened in light appearance. Dark was selected through Settings
  to match the requested design. Both appearances render; no profile was reset.
- Viewports: 1280x800, 1920x1080 and 800x800 recovery. Screenshots remain outside Git:
  `E:\Larry Lee\Morpheus\Review\1.0.1`.
- Live microphone recognition, paid neural voice quality and provider-backed
  conversations were not tested. Command Center reported its planning provider
  as not configured. No credentials were added or changed.

## Installer

`E:\Larry Lee\Morpheus\Releases\Windows\1.0.1\Morpheus-1.0.1-win-x64.exe`

263,685,547 bytes. SHA-256:
`90A24F92509DBE32FBFDC7AF15F44824C49482CEA3E1426A57D8467ADD7C05E1`.
Authenticode: `NotSigned`. Built with `pnpm package:win` from the committed runtime.
The copied delivery digest matches the source installer. The prior 1.0.0 delivery
is preserved; this build uses a separate version directory.
